[project]
name = "graveyard"
version = "0.1.0"
description = "Just the fucking code in python"
readme = "README.md"
authors = [
    { name = "Anupam", email = "anupaminit@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
graveyard = "graveyard:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
